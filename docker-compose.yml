version: "3.7"

services:
    app:
        image: cstable
        build:
            context: .
            dockerfile: dockerfile
        working_dir: /app
        volumes:
            - ./:/app
        stdin_open: true
        tty: true